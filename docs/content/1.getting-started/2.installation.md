---
title: Installation
description: Install and configure Nuxt Convex in your project.
---

# Installation

Add Nuxt Convex to your project with the required peer dependencies.

## Setup

Install the module and its peer dependencies:

```bash [Terminal]
pnpm add nuxt-convex convex @convex-vue/core
```

Add the module to your Nuxt configuration:

```ts [nuxt.config.ts]
export default defineNuxtConfig({
  modules: ['nuxt-convex'],
  convex: {
    storage: true, // Enable file storage (optional)
  },
})
```

## Environment Variables

The module reads `CONVEX_URL` or `NUXT_PUBLIC_CONVEX_URL` automatically. Run `npx convex dev` to start the Convex development server — this sets the environment variable for you.

| Variable                 | Description                                     |
| ------------------------ | ----------------------------------------------- |
| `CONVEX_URL`             | Convex deployment URL (set by `npx convex dev`) |
| `NUXT_PUBLIC_CONVEX_URL` | Alternative, follows Nuxt convention            |

## Virtual Modules

The module provides three virtual imports:

| Import            | Purpose                              |
| ----------------- | ------------------------------------ |
| `#convex`         | Core composables and client          |
| `#convex/api`     | Generated Convex API types           |
| `#convex/storage` | Storage operations (when enabled)    |

```ts
import { api } from '#convex/api'
import { useConvexQuery } from '#convex'
```

## Next Steps

With the module installed, you can:

- [Define your database schema](/backend/schema)
- [Create queries to read data](/backend/queries)
- [Use composables in Vue components](/composables/use-convex-query)
