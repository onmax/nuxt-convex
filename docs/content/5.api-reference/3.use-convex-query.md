---
title: useConvexQuery
description: Subscribe to Convex queries with SSR support, hydration-aware caching, and realtime updates.
---

`useConvexQuery` is the primary read API in both packages. It fetches once during SSR when enabled, then subscribes to the same query on the client.

```vue [app/pages/tasks.vue]
<script setup lang="ts">
import { api } from '#convex/api'

const { data, error, isPending, isSkipped, suspense } = useConvexQuery(
  api.tasks.list,
  { userId: 'user_123' },
)

await suspense()
</script>
```

## Signature

```ts
function useConvexQuery<Query extends FunctionReference<'query'>>(
  query: Query,
  args: MaybeRefOrGetter<FunctionArgs<Query> | 'skip'>,
  options?: {
    server?: boolean
  },
): {
  data: Readonly<Ref<FunctionReturnType<Query> | undefined>>
  error: Readonly<Ref<Error | null>>
  isPending: ComputedRef<boolean>
  isSkipped: ComputedRef<boolean>
  suspense: () => Promise<FunctionReturnType<Query> | undefined>
}
```

## Important behavior

- `server` defaults to the shared plugin or module setting.
- On the server, the composable uses `ConvexHttpClient`.
- On the client, the composable subscribes with `client.onUpdate(...)`.
- `suspense()` resolves immediately when data is already available.
- Pass `"skip"` as args to disable the subscription. `data` becomes `undefined` and `isSkipped` becomes `true`.

## Reactive arguments

```vue [app/pages/tasks.vue]
<script setup lang="ts">
import { api } from '#convex/api'

const userId = ref('user_123')

const { data } = useConvexQuery(
  api.tasks.list,
  computed(() => ({ userId: userId.value })),
)
</script>
```

## Conditional queries

```ts
const { data, isSkipped } = useConvexQuery(
  api.tasks.list,
  computed(() => userId.value ? { userId: userId.value } : 'skip'),
)
```

## Client-only mode

```ts
const { data } = useConvexQuery(api.tasks.list, { userId: 'demo' }, { server: false })
```

Use client-only mode for private dashboards or auth-sensitive queries.

## Related APIs

- [`useConvexController`](/api-reference/use-convex-controller)
- [`useConvexPaginatedQuery`](/api-reference/use-convex-paginated-query)
- [`useConvexQueries`](/api-reference/use-convex-queries)
- [`ConvexQuery`](/api-reference/convex-query)
