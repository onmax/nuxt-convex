{
  "name": "nuxt-convex-playground",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @onmax/convex-vue build && pnpm --filter nuxt-convex build && nuxi dev --dotenv .env.local",
    "build": "pnpm --filter @onmax/convex-vue build && pnpm --filter nuxt-convex build && nuxi build --dotenv .env.local"
  },
  "dependencies": {
    "@convex-dev/better-auth": "catalog:convex",
    "@convex-dev/r2": "catalog:convex",
    "@nuxthub/core": "catalog:playground",
    "@onmax/nuxt-better-auth": "catalog:playground",
    "better-auth": "catalog:playground",
    "convex": "catalog:convex"
  },
  "devDependencies": {
    "@nuxt/ui": "catalog:",
    "nuxt": "catalog:",
    "nuxt-convex": "workspace:*",
    "tailwindcss": "catalog:",
    "wrangler": "catalog:playground"
  }
}
